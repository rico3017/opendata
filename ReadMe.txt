Requirements:
    Python 3.5+
    MySQL 5.5+
Deployment process:
    1. Imports data to MySQL from the data directory;
    2. Run "pip install -r requirements" in the project directory;
    3. Run "python run.py";
    3. Open a browser and go to "http://localhost:5000/home".
